<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= data.title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1><%= data.title %></h1>
        <div id="table_sp" style="display: flex; flex-direction: column; justify-content: center; margin-top: 20px;">
            <div style="margin-bottom: 20px;">
                <h5 style="text-align: center;">Chi tiết đơn hàng</h5>
                <% if(data.detailCarts && data.detailCarts.length > 0) { %>
                    <h6>Mã đơn hàng: <%= data.detailCarts[0].madh %></h6>
                    <h6>Tên khách hàng: <%= data.detailCarts[0].donhang.user.hoso.fullname %></h6>
                    <h6>Ngày đặt: <%= new Date(data.detailCarts[0].donhang.ngaydat).toLocaleDateString('vi-VN') %> 
                        <%= new Date(data.detailCarts[0].donhang.ngaydat).toLocaleTimeString('vi-VN') %></h6>
                    <h6>Trạng thái: <%= data.detailCarts[0].donhang.trangthai %></h6>
                <% } %>
            </div>
            <table class="table table-sm">
                <thead>
                    <tr>
                        <th scope="col">Tên sản phẩm</th>
                        <th scope="col">Giá sản phẩm</th>
                        <th scope="col">Số lượng</th>
                    </tr>
                </thead>
                <tbody>
                    <% if(data.detailCarts) { %>
                        <% for(let i = 0; i < data.detailCarts.length; i++) { %>
                            <tr>
                                <td>
                                    <%= data.detailCarts[i].sanpham.tensp %>
                                </td>
                                <td>
                                    <%= data.detailCarts[i].gia %> đ
                                </td>
                                <td>
                                    <%= data.detailCarts[i].soluong %>
                                </td>
                            </tr>
                        <% } %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
